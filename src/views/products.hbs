<h1>Lista de productos (Home)</h1>

<table border="1" cellpadding="8" cellspacing="0">
    <thead>
        <tr>
        <th>Nombre</th>
        <th>Precio</th>
        <th>Categoría</th>
        <th>Stock</th>
        </tr>
    </thead>
    <tbody>
        {{#each products}}
        <tr>
            <td>{{this.title}}</td>
            <td>${{this.price}}</td>
            <td>{{category}}</td>
            <td>{{stock}}</td>
        </tr>
        {{/each}}
    </tbody>
</table>